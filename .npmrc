# Allow postinstall scripts for packages that need them to download/install
# platform-specific binaries. npm 11+ blocks scripts by default.
allow-scripts=esbuild,electron,prisma,@prisma/engines,@prisma/client,@embedded-postgres/windows-x64,embedded-postgres
fund=false
audit=false